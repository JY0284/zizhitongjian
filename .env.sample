# Copy to .env and fill in values (do NOT commit .env)

# DeepSeek API key for `knowledge_extraction.py`
DEEPSEEK_API_KEY=

# Default extraction model. Use deepseek-v4-pro for reasoning-heavy passes.
DEEPSEEK_MODEL=deepseek-v4-flash

# Amap (Gaode) Web Service key for geocoding
AMAP_KEY=
